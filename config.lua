return {
    -- Altezza massima di costruzione
    build_limit = 310,

    -- Area di controllo rimozione mesecons sotto il limite
    mesecons_limit = 20,

    -- Impedisci il posizionamento di nodi "mesecon"
    deny_mesecons = true,

    -- Intervallo in secondi per il controllo automatico nel globalstep
    check_interval = 2,
}
